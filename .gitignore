# Dependencies
/node_modules

# Build output
/.next
/next-env.d.ts
tsconfig.tsbuildinfo

# Test output
/coverage

# Secrets — every local env file stays out of git; the template does not.
.env
.env.*
!.env.example

# Machine-local agent settings only; the skills and commands are the rules CI
# and collaborators work to, so they stay in git.
.claude/settings.local.json

# OS noise
.DS_Store
